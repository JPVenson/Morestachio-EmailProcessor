<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:import="clr-namespace:Morestachio.MailProcessor.Client.Services.DataDistributor.Strategies"
                    xmlns:lex="http://wpflocalizeextension.codeplex.com"
                    xmlns:resources="clr-namespace:Morestachio.MailProcessor.Client.Resources"
                    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls">
    <DataTemplate DataType="{x:Type import:MailDistributorSelectorViewModel}">
        <resources:FieldsetGrid>
            <Label Content="{lex:Loc MailDistributor.Selector.SelectTitle}"/>
            <mah:SplitButton ItemsSource="{Binding MailDistributors}"
                             SelectedItem="{Binding SelectedDistributor}"
                             Grid.Column="1">
                <mah:SplitButton.ItemTemplate>
                    <DataTemplate DataType="{x:Type import:IMailDistributorBaseViewModel}">
                        <Label Content="{Binding Name}"/>
                    </DataTemplate>
                </mah:SplitButton.ItemTemplate>
            </mah:SplitButton>


            <!--<ComboBox ItemsSource="{Binding MailDistributors}"
                     SelectedItem="{Binding SelectedDistributor}"
                     Grid.Column="1">
                <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type import:IMailDistributorBaseViewModel}">
                        <Label Content="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>-->
        </resources:FieldsetGrid>
    </DataTemplate>  
</ResourceDictionary>